{
  "name": "trade-bot",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "repository": "https://github.com/pkempec/trade-bot.git",
  "scripts": {
    "install": "yarn --cwd trader install && yarn --cwd ui-frontend install && yarn --cwd ui-backend install",
    "start-trader": "yarn --cwd trader start",
    "start-simulation": "yarn --cwd trader simulate",
    "start-ui-backend": "yarn --cwd ui-backend start",
    "start-ui-frontend": "yarn --cwd ui-frontend start"
  },
  "dependencies": {}
}
